#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: the first integer to consider
 * @b: the second integer to consider
 *
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
