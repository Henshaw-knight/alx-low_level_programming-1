#include <stdio.h>

/**
 * main - Entry point
 **
 * Description: 'Program that prints the expected word in the exact way'
 **
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
